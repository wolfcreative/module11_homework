const mathNumbers = (x, n) => console.log(x ** n);

mathNumbers(6, 7);

// Скорее всего, неверно поняли задание. По условию задачи функция должна возвращать x в степени n, а у вас функция считает их произведение. Для вычисления степени можно использовать оператор ** или Math.pow(). Выше исправила